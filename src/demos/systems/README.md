# systems/

One folder per demo lives here, e.g.:

```
systems/
  whatsapp-to-dashboard/
    index.tsx   ← the demo screen (mock data only)
    data.ts     ← that demo's mock data
```

Nothing built yet — see `../registry.ts` for the planned shortlist and `../README.md`
for the ground rules. We build the first one once the anchor vertical is chosen.
