# shell/

The **reusable demo shell** goes here — the common frame every demo screen sits in
(browser chrome, sidebar, topbar, card grid). Build it ONCE, then each demo in
`../systems/` just drops its content inside.

Not built yet — deliberately holding off on UI/visual work until we pick the anchor
vertical and design it against the design kit (`~/.claude/design-kit`). No AI slop.
